package icons

templ CircleUserRound(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<path d="M18 20a6 6 0 0 0-12 0"></path>
		<circle cx="12" cy="10" r="4"></circle>
		<circle cx="12" cy="12" r="10"></circle>
	}
}
