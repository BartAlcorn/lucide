package icons

templ Mars(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<path d="M16 3h5v5"></path>
		<path d="m21 3-6.75 6.75"></path>
		<circle cx="10" cy="14" r="6"></circle>
	}
}
