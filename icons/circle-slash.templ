package icons

templ CircleSlash(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<circle cx="12" cy="12" r="10"></circle>
		<line x1="9" x2="15" y1="15" y2="9"></line>
	}
}
