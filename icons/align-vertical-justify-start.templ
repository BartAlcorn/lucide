package icons

templ AlignVerticalJustifyStart(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="14" height="6" x="5" y="16" rx="2"></rect>
		<rect width="10" height="6" x="7" y="6" rx="2"></rect>
		<path d="M2 2h20"></path>
	}
}
