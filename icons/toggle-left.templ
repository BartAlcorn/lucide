package icons

templ ToggleLeft(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<circle cx="9" cy="12" r="3"></circle>
		<rect width="20" height="14" x="2" y="5" rx="7"></rect>
	}
}
