package icons

templ AlignJustify(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<path d="M3 12h18"></path>
		<path d="M3 18h18"></path>
		<path d="M3 6h18"></path>
	}
}
