package icons

templ Dock(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<path d="M2 8h20"></path>
		<rect width="20" height="16" x="2" y="4" rx="2"></rect>
		<path d="M6 16h12"></path>
	}
}
