package icons

templ StretchVertical(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="6" height="20" x="4" y="2" rx="2"></rect>
		<rect width="6" height="20" x="14" y="2" rx="2"></rect>
	}
}
