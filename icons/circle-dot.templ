package icons

templ CircleDot(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<circle cx="12" cy="12" r="10"></circle>
		<circle cx="12" cy="12" r="1"></circle>
	}
}
