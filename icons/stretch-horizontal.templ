package icons

templ StretchHorizontal(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="20" height="6" x="2" y="4" rx="2"></rect>
		<rect width="20" height="6" x="2" y="14" rx="2"></rect>
	}
}
