package icons

templ Clock6(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<circle cx="12" cy="12" r="10"></circle>
		<polyline points="12 6 12 12 12 16.5"></polyline>
	}
}
