package icons

templ Speaker(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="16" height="20" x="4" y="2" rx="2"></rect>
		<path d="M12 6h.01"></path>
		<circle cx="12" cy="14" r="4"></circle>
		<path d="M12 14h.01"></path>
	}
}
