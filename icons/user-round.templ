package icons

templ UserRound(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<circle cx="12" cy="8" r="5"></circle>
		<path d="M20 21a8 8 0 0 0-16 0"></path>
	}
}
