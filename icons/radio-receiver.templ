package icons

templ RadioReceiver(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<path d="M5 16v2"></path>
		<path d="M19 16v2"></path>
		<rect width="20" height="8" x="2" y="8" rx="2"></rect>
		<path d="M18 12h.01"></path>
	}
}
