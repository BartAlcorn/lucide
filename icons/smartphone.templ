package icons

templ Smartphone(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="14" height="20" x="5" y="2" rx="2" ry="2"></rect>
		<path d="M12 18h.01"></path>
	}
}
