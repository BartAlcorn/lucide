package icons

templ Timer(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<line x1="10" x2="14" y1="2" y2="2"></line>
		<line x1="12" x2="15" y1="14" y2="11"></line>
		<circle cx="12" cy="14" r="8"></circle>
	}
}
