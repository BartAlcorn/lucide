package icons

templ SquareCheck(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect width="18" height="18" x="3" y="3" rx="2"></rect>
		<path d="m9 12 2 2 4-4"></path>
	}
}
