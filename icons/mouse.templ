package icons

templ Mouse(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	@SVG(p) {
		<rect x="5" y="2" width="14" height="20" rx="7"></rect>
		<path d="M12 6v4"></path>
	}
}
